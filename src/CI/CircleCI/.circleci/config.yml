version: 2
jobs:
  build:
    docker:
      - image: xinyaanzyoy/pipelinetest2351:latest
    steps:
      - run:
          name: Xbooks_Transformation
          command: cd /Xbooks/ && source activate Xbooks_env && python Xbooks.py $REPO $PAT